apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "cloudpa-6605"
    labels:
        app: "cloudpa-6605"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: default-cloudpa-6605.cfeffc71f0064612a62c.uksouth.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "cloudpa-6605"
                servicePort: 80