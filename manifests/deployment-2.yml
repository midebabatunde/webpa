apiVersion : apps/v1
kind: Deployment
metadata:
  name: "cloudpa-5a75"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "cloudpa-5a75"
  template:
    metadata:
      labels:
        app: "cloudpa-5a75"
    spec:
      containers:
        - name: "cloudpa-5a75"
          image: "cloudpa.azurecr.io/cloudpa"
          ports:
          - containerPort: 80