apiVersion : apps/v1
kind: Deployment
metadata:
  name: "cloudpa-8037"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "cloudpa-8037"
  template:
    metadata:
      labels:
        app: "cloudpa-8037"
    spec:
      containers:
        - name: "cloudpa-8037"
          image: "cloudpa.azurecr.io/cloudpa"
          ports:
          - containerPort: 80