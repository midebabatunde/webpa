apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "cloudpa-8037"
    labels:
        app: "cloudpa-8037"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: default-cloudpa-8037.cfeffc71f0064612a62c.uksouth.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "cloudpa-8037"
                servicePort: 80