apiVersion: v1
kind: Service
metadata:
    name: "cloudpa-8037"
    labels:
        app: "cloudpa-8037"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "cloudpa-8037"