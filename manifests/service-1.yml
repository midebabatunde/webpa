apiVersion: v1
kind: Service
metadata:
    name: "cloudpa-6605"
    labels:
        app: "cloudpa-6605"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "cloudpa-6605"