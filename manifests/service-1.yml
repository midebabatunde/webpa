apiVersion: v1
kind: Service
metadata:
    name: "cloudpa-fffa"
    labels:
        app: "cloudpa-fffa"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "cloudpa-fffa"